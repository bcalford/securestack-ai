# Sample Findings

SecureStack AI can identify heuristic indicators such as hardcoded secrets, `.env` secrets, wildcard CORS, possible SQL injection, command execution, wildcard IAM permissions, public S3 settings, open administrative security groups, insecure Dockerfile patterns, and suspicious dependency scripts.

Findings are defensive triage signals and should be validated before remediation work is prioritized.
