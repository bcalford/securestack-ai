export type Filters = { search: string; severity: string; category: string; status: string; confidence: string; sortBy: string };
export default function FindingFilters({ filters, setFilters, categories }: { filters: Filters; setFilters: (f: Filters) => void; categories: string[] }) { return <div className="filters"><input aria-label="Search findings" className="input" placeholder="Search findings" value={filters.search} onChange={e=>setFilters({...filters,search:e.target.value})}/><select aria-label="Filter severity" value={filters.severity} onChange={e=>setFilters({...filters,severity:e.target.value})}><option value="">All severities</option><option>CRITICAL</option><option>HIGH</option><option>MEDIUM</option><option>LOW</option><option>INFO</option></select><select aria-label="Filter category" value={filters.category} onChange={e=>setFilters({...filters,category:e.target.value})}><option value="">All categories</option>{categories.map(c=><option key={c}>{c}</option>)}</select><select aria-label="Filter status" value={filters.status} onChange={e=>setFilters({...filters,status:e.target.value})}><option value="">All statuses</option><option>OPEN</option><option>REVIEWED</option><option>FALSE_POSITIVE</option><option>FIXED</option></select><select aria-label="Filter confidence" value={filters.confidence} onChange={e=>setFilters({...filters,confidence:e.target.value})}><option value="">All confidences</option><option>HIGH</option><option>MEDIUM</option><option>LOW</option></select><select aria-label="Sort findings" value={filters.sortBy} onChange={e=>setFilters({...filters,sortBy:e.target.value})}><option value="priority">Sort by priority</option><option value="severity">Sort by severity</option><option value="file">Sort by file</option><option value="status">Sort by status</option></select></div>; }
