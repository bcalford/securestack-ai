# vulnerable-spring-api

This sample is intentionally designed for SecureStack AI demos. It uses fake placeholder values only and must not be deployed.

## Expected findings
Run a STANDARD or FULL scan against this folder. Vulnerable samples should trigger findings for secrets, CORS, authentication/authorization, logging, dependency, container, or cloud/IaC rules depending on the files present. The clean example should produce a low-risk result with no high-risk intentional findings.
