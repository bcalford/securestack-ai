import type { PastedFile } from '../../types';
export default function PastedFileEditor({ files, setFiles }: { files: PastedFile[]; setFiles: (files: PastedFile[]) => void }) { return <>{files.map((f,i)=><div className="card" key={i}><label>File name<input className="input" value={f.fileName} onChange={e=>setFiles(files.map((v,j)=>j===i?{...v,fileName:e.target.value}:v))}/></label><label>Language/type<input className="input" value={f.fileType} onChange={e=>setFiles(files.map((v,j)=>j===i?{...v,fileType:e.target.value}:v))}/></label><label>Paste text<textarea rows={8} value={f.content} onChange={e=>setFiles(files.map((v,j)=>j===i?{...v,content:e.target.value}:v))}/></label></div>)}</>; }
