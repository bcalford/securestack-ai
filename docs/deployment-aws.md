# AWS Deployment Notes

Production AWS deployment automation is intentionally future work. A later pass can add container registry publishing, ECS/App Runner infrastructure, managed database options, TLS, logging, and secrets management.

Current implementation uses mock AI by default. Bedrock/OpenAI provider configuration is not implemented as a required runtime dependency in this release pass.
